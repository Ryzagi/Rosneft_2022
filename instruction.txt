1. Создать энвайромент с помощью любого менеджера энвайроментов.
Например с помощью conda:

conda create --name env python=3.9 && conda activate env

2. Установить requirements.txt

pip install -r ./libs/requirements.txt

3. Запустить код (из корня):

python ./code/run.py